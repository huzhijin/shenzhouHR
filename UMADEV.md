# UMADEV.md — brownfield boundary brief

This is an **existing** project adopted by UmaDev. Work **incrementally**: change the smallest surface that satisfies the requirement, match the conventions already in this codebase, and never regenerate or rewrite files wholesale.

## Detected stack

- Frontend: React 19.2, TypeScript 5.9, Vite 8.1, Ant Design 6.5, React Router 7.18, Vitest, ESLint
- Backend: Java 21, Spring Boot 4.1, Maven Wrapper, Spring Security, MyBatis, Flyway
- Database baseline: MySQL-compatible schema and MariaDB JDBC driver; verify the final production database decision against the V1.9 specification before implementation
- Dev server: Vite dev server under `frontend/`
- Adopt limitation: UmaDev 1.0.64 did not detect nested manifests from the workspace root; the stack above was verified directly from `frontend/package.json` and `backend/pom.xml`

## Commands (run these, do not invent new ones)

```bash
# Frontend full check
cd frontend
npm run check

# Frontend development/demo
npm run dev
npm run demo

# Backend tests
cd ../backend
./mvnw test
```

Use `npm ci` only when dependencies need a clean reinstall. Do not commit or rely on `frontend/node_modules`, `frontend/dist`, or `backend/target` as source artifacts.

## Boundaries (hard rules)

- Do not reformat or rewrite files unrelated to the task.
- Reuse existing modules, helpers, and patterns before adding new ones.
- Frontend API calls must match the adopted contract in `.umadev/contracts/openapi.yaml` (FE↔BE alignment).
- Keep public APIs and on-disk formats backward-compatible unless asked.
- Treat `design/open-design/v1.9/` as the confirmed V1.9 visual and interaction handoff. Do not copy its static HTML wholesale into React; map routes, tokens, reusable components, states, and responsive behavior first.
- The active product baseline is V1.9. Old V1.7 requirements and artifacts are historical context only and must not reintroduce SSO uncertainty, ongoing organization synchronization, old annual-leave rules, or payroll frontend routes.
- Keep payroll frontend completely undiscoverable in the current release; backend payroll work, if authorized later, remains separately permissioned and future-reserved.

## Key decisions (non-obvious context)

- 165 source file(s) are indexed for retrieval under `.umadev/project-source-index/`; prefer searching existing code over guessing.
- This brief is generated by `umadev adopt`; the authoritative project README and in-repo docs still apply.
