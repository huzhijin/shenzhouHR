# 神州 HR V1.9 · QA

对象：`V19-OD-00` 项目设计 brief  
检查日期：2026-07-22

## 范围门禁

- [x] V19-OD-00 生成轮次只新增项目 brief，当时未生成后续产品页面。
- [x] 旧 artifact 未删除、未覆盖、未批量重画；PRECHECK 原文件保留。
- [x] 旧编号统一引用为 `LEGACY-V17-OD-xx`。
- [x] 所有未读取旧 artifact 标记 `MISSING / UNKNOWN`，未虚报完成。
- [x] 薪资相关旧产物统一 `FUTURE_RESERVED`，当前 IA、路由与组件中不可发现。
- [x] 未出现组织同步导航；OA 考勤业务单据仍保留为只读数据来源。

## 内容核验

- [x] 包含旧 artifact 审计和迁移建议。
- [x] 包含八个 PC 一级模块、移动四项 IA 和建议路由。
- [x] 包含七类角色 × 页面域 × 动作权限矩阵。
- [x] 包含公共组件复用矩阵与页面状态矩阵。
- [x] 包含 V1.9 指标口径与 artifact-index。
- [x] `PROJECT_EMBEDDED` 与平台 `NOT_ACTIVE` 如实区分。
- [x] V19-OD-00 是索引中唯一 `ACTIVE_BASELINE`；PRECHECK 为 `SUPERSEDED`。
- [x] 路由、API、权限和性能相关内容标记为工程假设或未验证。

## 响应式与可访问性静态检查

- [x] 桌面：1366×768、1440×900、1920×1080 的双栏文档布局有明确断点。
- [x] 平板：1024×768、768×1024 使用折叠导航和可触控目录控件。
- [x] 手机：360×800、390×844、430×932 将宽表格重排为标签卡片，无固定桌面宽度。
- [x] 可交互控件最小高度 44px，提供 `:focus-visible`。
- [x] 状态均含文本，非仅依赖颜色。
- [x] 支持 `prefers-reduced-motion`。
- [x] 无 `scrollIntoView`、无外链资源、无横向滚动依赖。

## Critique / Refine

第一轮发现：17 行旧产物表与多矩阵在窄屏会造成密度过高，且 `MISSING` 与迁移建议的层级不够清楚。

本轮修正：

1. 将桌面侧栏在 1024px 以下改为顶部选择目录。
2. 将所有矩阵在 820px 以下重排为带字段标签的单列记录卡。
3. 将“文件状态”和“迁移建议”拆成独立列，避免把 `MISSING / UNKNOWN` 误读为处理建议。
4. 为状态徽标增加文字、描边和可访问名称，不仅使用颜色。
5. 将关键边界固定到页首基线条：V1.9、PROJECT_EMBEDDED、平台 NOT_ACTIVE、薪资前台不可发现。

可视导出说明：宿主当前 CLI 未提供 `export` 命令，因此本轮未生成预览截图；已完成 HTML 结构、断点、溢出与交互静态检查，未将截图状态误报为已验证。

结论：P0 通过；P1 静态项通过。V19-OD-00 可作为后续唯一活动基线，当前交付无阻断项。

---

# V19-OD-01 · 本地登录与全局壳层 QA

对象：`v19-od-01-login-shell.html`  
检查日期：2026-07-22

## 范围与业务门禁

- [x] 使用本系统独立账号密码，不出现 SSO/IdP 登录入口。
- [x] 包含显示/隐藏密码、忘记密码/联系管理员、首次改密、密码过期、失败锁定、会话过期和安全退出。
- [x] PC 导航只包含 V1.9 八个业务域；没有组织持续同步入口。
- [x] 手机底栏仅“今日、记录、反馈、我的”四项。
- [x] 未生成工作台指标或后续业务页面；未设计区域使用诚实边界说明。
- [x] 页面中无被排除模块的导航、路由、卡片、搜索结果、占位或暗示。

## 状态与交互

- [x] 登录必填错误与字段关联，错误摘要可聚焦。
- [x] 密码可见性按钮更新 `aria-label` 与 `aria-pressed`。
- [x] 密码修改校验当前密码、长度和两次输入一致性。
- [x] 403 不泄露对象是否存在；404 提供 request_id 与返回路径。
- [x] 维护、断网只读、数据过期均含原因、影响和恢复动作。
- [x] 对话框有焦点进入、Tab 循环、Esc 关闭和关闭后焦点恢复；会话过期不可用 Esc 绕过。
- [x] 角色切换器明确标注“仅用于演示”，不暗示真实权限由客户端决定。

## 响应式与无障碍

- [x] PC 1366/1440/1920 使用固定顶栏和左导航。
- [x] 平板 768/1024 使用抽屉导航与遮罩关闭。
- [x] 手机 360/390/430 使用安全区底栏、单列内容和底部对话框。
- [x] 主要交互目标至少 44px；键盘可完成登录、导航、保存新密码、取消和退出。
- [x] `:focus-visible`、`aria-live`、文字状态和 `prefers-reduced-motion` 已覆盖。
- [x] 无外部资源、无 `scrollIntoView`、无固定桌面内容宽度导致的移动横向滚动。

## Critique / Refine

第一轮发现：如果把所有认证和系统状态都暴露为页面标签，会把原型控制器混入生产信息架构；同时平板顶栏的范围与数据时间会挤压搜索框。

修正：

1. 把状态切换收进默认关闭、明确标注“仅用于演示”的独立面板。
2. 1100px 以下切换抽屉导航，860px 以下隐藏非必要顶栏状态，640px 以下改为四项底栏。
3. 断网状态改为只读并禁示提交语义，数据过期提供显式刷新，避免只用颜色告警。
4. 会话过期使用不可绕过确认层，退出使用危险操作样式并提供取消路径。
5. 后续模块点击只显示交付边界，不虚构业务指标或页面内容。

---

# V19-OD-02～14 · Web Prototype 批次 QA

对象：14 个独立 artifact、共享壳层、路由注册表、演示数据与五条主链路  
检查日期：2026-07-22

## 批次范围门禁

- [x] V19-OD-02～14 均为独立 HTML 文件，未合并为巨型页面。
- [x] V19-OD-00 仍为唯一 `ACTIVE_BASELINE`；V19-OD-00、V19-OD-01 和 LEGACY-V17 文件未重画。
- [x] V19-OD-01 只补充登录成功后按演示角色跳转到 OD02 的关系。
- [x] V19-OD-15、V19-OD-16 未生成页面、路由、缩略图或占位。
- [x] `PROJECT_EMBEDDED` 如实使用；未声称平台已激活 Design System。
- [x] 产品前台未出现被排除模块的导航、路由、搜索结果、报表、空状态或移动入口。
- [x] 组织/员工期初、时间账户期初、离线原始打卡保持三个独立入口和发布规则。
- [x] OA 只读业务单据与在线打卡保留；不存在组织持续或双向同步入口。

## 逐项 QA / Critique / Refine

| Artifact | P0 | QA / 交互覆盖 | 本轮 Critique → Refine |
|---|---|---|---|
| V19-OD-02 | PASS | 七角色、三级范围、联动待办、角色切换防闪现 | 首轮工作台指标对所有角色过于同质；改为角色专属指标与待办，并随批次、异常和月结状态更新。 |
| V19-OD-03 | PASS | 模板、上传、映射、预检、差异、发布、错误与恢复 | 首轮六步向导在手机端横向拥挤；改为可换行阶段条与单列错误记录，保留具体发布动作。 |
| V19-OD-04 | PASS | 本地组织树、员工、任职周期、二次入职、累计工龄审计 | 首轮三栏在平板密度过高；改为树/列表/详情分层，移动端单列，并在期初未发布时给出明确返回导入路径。 |
| V19-OD-05 | PASS | 模板、版本、范围、生效期、冲突、试算、审计、回滚 | 首轮列表与生命周期竞争主层级；将版本状态和下一动作提到首屏，历史版本退为次级标签。 |
| V19-OD-06 | PASS | 考勤组、期限分配、跨日班次、夏冬令版本、工作日历 | 首轮版本差异难扫描；增加时间轴与当前/下一版本对照，缩短长文案避免 1366px 溢出。 |
| V19-OD-07 | PASS | 晚餐默认启用、1 次/15 分钟宽限、7 日单边缺卡、试算发布 | 首轮边界规则藏在表单中；拆为三个策略标签和解释性试算，发布确认列明范围、人数、期间和可逆性。 |
| V19-OD-08 | PASS | 假别配置、年假资格、累计工龄档位、周年发放与到期 | 首轮资格与档位易被误读为同一判断；改为并列判定链，并完整展示资格、工龄、档位、日期、天数和小时。 |
| V19-OD-09 | PASS | 账户列表、独立余额导入、来源批次、流水、冻结、幂等 | 首轮未发布批次仍显示最终调休余额；改为余额随发布状态更新，并隐藏未入账来源流水。 |
| V19-OD-10A | PASS | 模板版本、厂商映射、人员匹配、预检、跨来源去重、冻结、发布、追溯 | 首轮统计和错误表在窄屏超载；改为统计卡分组、字段标签卡片和独立高风险发布层。 |
| V19-OD-10B | PASS | 在线设备、OA 只读单据、水位、失败重试、来源隔离 | 首轮容易把 Excel 预检事实误读为在线正式来源；增加批次发布状态联动和来源类型标签。 |
| V19-OD-11 | PASS | 工作段、原始打卡、OA、规则版本、计算结果与“为什么” | 首轮直接访问时可能把未发布 Excel 当正式证据；增加“仅供导入复核”提示并在批次发布后自动消失。 |
| V19-OD-12 | PASS | 异常、证据、人工调整、重算差异、月结阻断、报表 | 首轮月结主动作未充分解释阻断；改为阻断驱动按钮与返回离线批次路径，完成后同步报表状态。 |
| V19-OD-13 | PASS | 今日、记录、反馈、我的；移动月历、解释、年假与反馈 | 首轮桌面卡片在手机上信息层级偏平；调整为移动优先单列与渐进披露，触控目标保持 44px。 |
| V19-OD-14 | PASS | 本地账号、动作权限、数据范围、只读审计、任务与健康 | 首轮审计日志固定显示发布成功，与重置状态不一致；改为按共享状态显示预检/草稿/发布记录。 |

## 共同状态、响应式与无障碍

- [x] 共享演示面板可检查正常、加载、空数据、失败、403、冲突、冻结、处理中、部分成功和成功。
- [x] 页面特有状态包含导入错误/重复/冻结、规则冲突、缺卡待补正、重算差异、月结阻断和只读审计。
- [x] 1366/1440/1920 桌面保留高密表格；1024/768 平板使用导航抽屉；360/390/430 手机将表格重排为字段标签卡片。
- [x] 共享交互目标至少 44px；提供 `:focus-visible`、跳到主要内容、对话框焦点陷阱、Esc 关闭与关闭后恢复。
- [x] 状态均有文字，不只靠颜色；支持 `prefers-reduced-motion`。
- [x] 高风险确认层显示作用范围、记录/人数、期间、可逆性与明确动作名称。
- [x] 非主链路未实现动作会显示“原型未接入 / 待工程实现”，不伪装为已完成能力。

## 联动与一致性

- [x] `route-to-artifact.json` 与共享 `ROUTES` 一致，所有已注册文件存在。
- [x] `demo-data-registry.json` 与默认浏览器状态使用相同的部门、员工、期间、批次、规则、异常、余额和月结锚点。
- [x] 五条 demo-flow 主链路均有明确入口、目标、前置状态、状态变化、返回路径和重置方法。
- [x] `PROTOTYPE_ONLY` 演示控制条不进入正式导航；`PROTOTYPE_SIMULATION` 已在 handoff 标明。
- [x] 登录跳转、面包屑、指标、待办、列表行、向导、证据、月结阻断与返回入口已注册。
- [x] 静态 JavaScript 语法、HTML ID / `data-od-id` 唯一性、相对资源和禁止路由词检查通过。

结论：V19-OD-02～14 P0 全部通过；批次 critique 总分 4/5。真实后端、权限、接口、数据库事务、性能和安全保持 `ENGINEERING_ASSUMPTION / NOT_VERIFIED`。

批次最终静态检查结果：14/14 artifact 文件存在；34/34 注册路由均指向存在文件；共享 `ROUTES` 与 `route-to-artifact.json` 完全一致；HTML `id` / `data-od-id` 无重复；全部内联脚本与共享脚本通过语法检查；禁止模块关键词与 V19-OD-15/16 文件检查通过。宿主 CLI 当前不提供 `export` 命令，因此代表性页面的可视导出未执行，未将其误报为已验证；响应式结论来自结构、断点与溢出静态检查。

---

# V19-OD-15 · 独立考勤数据大屏 QA

对象：`v19-od-15-attendance-screen.html`  
检查日期：2026-07-22

## 基线与范围

- [x] V19-OD-00 仍是唯一 `ACTIVE_BASELINE`；V19-OD-01～14 保持 `COMPLETE`。
- [x] 当前表面为自由形式 Live artifact，未生成 V19-OD-16。
- [x] 使用 `PROJECT_EMBEDDED` 的 shenzhou-hr Logo、色彩、字体、间距和状态规则。
- [x] `/display/attendance` 是独立只读路由，不进入现有产品导航或员工端。
- [x] 页面没有个人、工号、精确位置、假别原因、敏感单据详情或被排除模块入口。

## 内容与口径

- [x] 展示出勤率、异常结构、加班趋势、部门对比和月结进度。
- [x] 展示离线厂区 A 最近成功导入时间、待处理批次及 ATT-XLS-DEMO-001 预检摘要。
- [x] 分别显示在线设备、OA 只读业务单据和离线 Excel 的来源水位。
- [x] 页头给出数据截至时间；指标口径层解释分子分母、去重、确认状态和刷新边界。
- [x] 所有数值为明确标注的合成演示聚合，并与 demo-data-registry 的 81 人、7 条异常、72% 月结和批次状态一致。
- [x] 浏览器存在 V19 演示状态时，只读同步离线批次发布和月结完成结果。

## 16:9、状态与可访问性

- [x] 使用 1920×1080 逻辑画布；3840×2160 等比 2× 缩放，不改变信息层级。
- [x] 非 16:9 窗口使用安全留白，不裁切、不拉伸、无滚动。
- [x] `loading / empty / failure / forbidden / stale / partial / normal` 状态均有文字，不只依赖颜色。
- [x] 图表使用实心面积、柱形和分段填充；没有只有轮廓的占位图表。
- [x] 范围切换、刷新、全屏和口径层可键盘操作；焦点可见，口径层支持 Esc 关闭。
- [x] 支持 `prefers-reduced-motion`，无装饰性循环动画。
- [x] 使用完整横版 Logo 和文本回退；无外部资源。

## Critique / Refine

第一轮发现：直接复用公司级月结阻断数，会在部门范围下同时显示“无待处理导入”和“阻断 3 项”，造成范围口径矛盾；异常分段和趋势图的可访问文本也未随范围变化。

本轮修正：

1. 月结阻断数改为按当前范围的待发布批次、异常和重算状态计算。
2. 离线批次摘要随授权范围切换为待处理、已发布或当前范围无待处理。
3. 趋势图说明、异常结构标签、人数、加班与刷新状态随范围同步。
4. 固定 1920×1080 逻辑画布并整体等比缩放，避免 4K 下密度漂移和卡片拉伸。
5. 将完整指标口径放入只读说明层，主屏保留监控层级和扫读速度。

结论：P0 通过；Critique 4/5。服务端聚合、实时刷新、权限、接口、性能和安全仍为 `ENGINEERING_ASSUMPTION / NOT_VERIFIED`。

---

# V19-OD-16 · 全项目 QA 与设计交接

对象：`V19-OD-00`～`V19-OD-16`  
检查日期：2026-07-22

## 文件、编号与基线

- [x] V19-OD-00～16 的当前交付文件全部存在；V19-OD-01～15 为 `COMPLETE`。
- [x] V19-OD-00 仍是索引中唯一 `ACTIVE_BASELINE`；PRECHECK 保持 `SUPERSEDED`。
- [x] 每个 HTML 的编号、`lang=zh-CN`、viewport、标题和主体语义完整。
- [x] 实际 DOM `id`、`data-od-id`、对话框目标、标签页目标和 ARIA 引用无冲突或缺失。
- [x] 共享 JavaScript 与全部内联脚本语法检查通过。

## 路由、链路与数据一致性

- [x] 36/36 注册 route 指向存在文件；0 个重复 route、0 个未知来源、0 个未知返回。
- [x] 共享 `ROUTES` 与 `route-to-artifact.json` 完全一致；产品导航没有未登记 route。
- [x] V19-OD-01 的七类角色均跳转 V19-OD-02 对应工作台；403 统一且不泄露对象存在性。
- [x] 五条 `demo-flow` 的全部节点已注册并可连续点击；0 个未说明主链路断点。
- [x] 2026-07、制造一部/研发一部/人力资源部、EMP-DEMO-001/002、ORG-INIT-DEMO-001、TIME-INIT-DEMO-001、ATT-XLS-DEMO-001、DEVICE-DEMO-01 与 ATT-POLICY-DEMO-V3 跨页面一致。
- [x] `PROTOTYPE_ONLY` 与 `PROTOTYPE_SIMULATION` 未被描述为真实后端或生产能力。

## 业务口径与失效口径

- [x] 独立账号密码、首次改密、密码过期、失败锁定、会话过期与退出完整。
- [x] 组织仅期初 Excel 导入，发布后本地维护；OA 考勤业务单据继续为只读来源。
- [x] 组织/员工期初、时间账户期初、离线原始打卡三类导入 route、模板、字段与发布规则分离。
- [x] 规则版本、范围、生效期、优先级、冲突、试算、发布、审计、回滚和冻结保护完整。
- [x] 季节班次、晚餐默认扣除、每自然月一次且 15 分钟内宽限、单边缺卡 7 日补正且逾期默认只影响对应工作段完整。
- [x] 假别默认工作日且配置化；年假资格、累计工龄档位、周年发放和二次入职重算完整。
- [x] 产品 artifact 与 route 扫描中：薪资 0、工资条 0、PAYROLL 0、`/payroll` 0、`/salary` 0。
- [x] 产品 artifact 扫描中：SSO 待定 0、IdP 待定 0、组织持续/定时/双向同步 0。

## 响应式、状态与无障碍

- [x] 共享 1180/900/720 断点覆盖 PC、平板与手机；720px 以下表格转字段卡片，移动底栏适配安全区。
- [x] OD15 采用 1920×1080 逻辑画布并等比覆盖 3840×2160。
- [x] 正常、加载、空、失败、403、冲突、冻结、处理中、部分成功与成功按模块适用性登记。
- [x] 主要颜色对比：正文/白 14.68:1、次要文字/白 4.76:1、品牌蓝/白 8.85:1、危险红/白 4.80:1。
- [x] 44px 目标、跳到主内容、可见焦点、状态文字、减少动态效果与对话框焦点陷阱通过静态检查。
- [x] 本轮修复共享对话框焦点恢复，并自动补全无标签关闭按钮的“关闭”可访问名称。
- [x] 宿主无可用 HTML 导出命令；响应式截图标为 `VISUAL_CAPTURE_NOT_AVAILABLE`，未伪报已完成像素级检查。

## MISSING / BLOCKED / NOT_VERIFIED

- `MISSING`：当前 V1.9 设计交付为 `NONE`；V1.7 原文件仍为历史 `MISSING / UNKNOWN`。
- `BLOCKED`：当前 V1.9 设计交付为 `NONE`。
- `NOT_VERIFIED`：后端鉴权、数据库隔离、真实接口、XLSX 处理、规则引擎、任务事务、性能、安全、监控与像素级截图。

## Critique / Refine

评分：4/5。终检发现共享弹窗关闭焦点恢复与无标签关闭按钮问题；已在 `assets/v19-prototype.js` 修正并再次完成脚本、路由、ARIA 与链路检查。未进行无法由宿主支持的像素级截图验证，因此不评为 5/5。

结论：`V19_OD_DESIGN_HANDOFF_COMPLETE`。
