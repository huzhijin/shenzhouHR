# 神州 HR V1.9 · MISSING / BLOCKED / NOT_VERIFIED 清单

更新日期：2026-07-22

## MISSING

- 当前 V1.9 设计交付：`NONE`。V19-OD-00～16 均存在；V19-OD-01～15 为 `COMPLETE`，V19-OD-00 保持唯一 `ACTIVE_BASELINE`。
- V1.7 原 artifact 文件：继续为 `MISSING / UNKNOWN`，仅作为 `LEGACY-V17-OD-xx` 历史引用，不影响 V1.9 完整性，也不允许推测内容。

## BLOCKED

- 当前 V1.9 设计交付：`NONE`。
- 响应式截图：宿主未提供可用的 Open Design HTML 导出命令，状态为 `VISUAL_CAPTURE_NOT_AVAILABLE`；静态断点与溢出检查已通过，人工截图清单已交付，不将截图缺失伪报为已验证。

## NOT_VERIFIED

以下能力未由 HTML 原型验证，不能宣称已实现：

1. 本地账号落库、密码策略、锁定阈值、会话、CSRF、防暴力破解和安全审计。
2. 服务端页面、对象、字段、动作、导出权限与数据范围隔离。
3. XLSX 解析、模板真实下载、病毒扫描、对象存储、幂等、发布事务和错误报告生成。
4. 得力设备 API、OA 考勤业务单据接口、水位、重试、隔离队列和来源真实性。
5. API/Excel 跨来源规范事件合并、重复判定和原始证据不可变存储。
6. 规则引擎、作用范围冲突、样例试算、版本回滚、历史重放和冻结保护。
7. 人工调整审批、重算队列、月结/反月结数据库事务和受控导出。
8. 数据库隔离、并发、真实性能、监控、备份恢复、消息通知和安全扫描。
9. 1920×1080、3840×2160 及全部响应式目标的浏览器像素级截图；当前只有静态布局证据。

## PROTOTYPE_SIMULATION

- `localStorage` 跨页状态、演示角色、保存草稿、预检、发布、补正、重算、月结、规则发布、反馈与重置均为客户端演示。
- `PROTOTYPE_ONLY` 控制条和 `/qa/handoff` 设计交接 route 不进入生产前台。

## 已确认需求

独立账号密码、组织期初导入后本地维护、OA 考勤单据只读、三类导入分离、季节班次版本、晚餐默认扣除、每自然月一次且 15 分钟内宽限、单边缺卡 7 日补正且默认只影响对应工作段、假别配置化、周年年假、冻结保护以及被排除模块前台完全不可发现，均属于当前 V1.9 已确认设计事实，不是原型推断。

