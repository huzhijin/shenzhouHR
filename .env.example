# Generated by UmaDev — copy to .env and fill in real values.

# Database
DATABASE_URL=postgresql://app:app@localhost:5432/app

# Auth
JWT_SECRET=change-me-to-a-real-secret
JWT_EXPIRES_IN=900

# Server
PORT=3000
NODE_ENV=development

# Node
npm_config_registry=https://registry.npmjs.org
# Redis
REDIS_URL=redis://localhost:6379
# SMTP (email)
SMTP_URL=smtp://localhost:587

# 8 API endpoints declared in              .umadev/contracts/openapi.yaml (review) / openapi.json (codegen)
