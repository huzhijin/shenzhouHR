# 查询报表穿透详情与异常列对齐 20260824-query-pages4

把 `shenzhouhr-release-20260824-query-pages4.tar.gz` 和 `upgrade-query-pages4.sh` 都传到 `/root` 后执行：

```bash
bash /root/upgrade-query-pages4.sh
```

不要覆盖 `/opt/shenzhouhr/start-prod.sh`。部署后浏览器 Ctrl+F5。
