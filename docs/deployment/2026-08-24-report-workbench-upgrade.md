# 查询报表口径、部门路径与工作台免打卡 20260824-report-workbench

把 `shenzhouhr-release-20260824-report-workbench.tar.gz` 和 `upgrade-report-workbench.sh` 都传到 `/root` 后执行：

```bash
bash /root/upgrade-report-workbench.sh
```

不要覆盖 `/opt/shenzhouhr/start-prod.sh`。部署后浏览器 Ctrl+F5。OPEN 月点「重新计算」。
