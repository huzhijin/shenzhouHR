## 1. 认人策略钉死

- [x] 1.1 核对 `EvidenceResolutionPolicy`：确认绑定唯一时必须赢过 empno；多绑定 / 无绑定时行为与 spec 一致
- [x] 1.2 核对 `DeliEplusClient`：长度 ≥ 16 的数字人员 id 才用目录覆盖 empno；短 CHECKIN `user_id`（含 387）不得当彭伟
- [x] 1.3 补测试：彭伟雪花 + empno SZST0289 → 有效事件在彭伟；387 + 周步新 empno → 仍是周步新

## 2. 绑定种子（先数据，再回放）

- [x] 2.1 从七月冲突工号映射抽出雪花账号 id → 花名册工号（陆玉蕾、彭伟、李扬、王善源至少这四人）
- [x] 2.2 用得力人员目录按工号+姓名交叉验证雪花 id；对不上的列入人工表，不自动写入
- [x] 2.3 编写可重复执行的绑定写入（`deli_user_id` / `deli_ext_id` confirmed，按 source 唯一）
- [x] 2.4 李扬若映射没有雪花 id：从目录按 `SZST0291`+姓名补一条后再回放

## 3. 历史回放

- [x] 3.1 实现按 source + 时间窗扫描 raw（含 QUARANTINED 与已 MATCHED）
- [x] 3.2 每条 `source_record_id` 用当前策略重算 match；员工变了则迁移 effective，隔离变匹配则补 effective
- [x] 3.3 同一 `source_record_id` 不得出现第二条有效事件；回放可重入
- [x] 3.4 回放默认窗 2026-08-01 00:00+08 至 2026-08-13 24:00+08，允许延到今天以消化隔离积压
- [x] 3.5 回放结束后打印仍隔离的 reason、empno、雪花 id，不得静默丢

## 4. 重算与验收

- [x] 4.1 回放若改过有效打卡，重算江苏神州 2026-08（以及回放碰到的其他公司月）
- [x] 4.2 用两份 Excel 互补 + 花名册姓名对齐跑全员首末对比（黄凯 667、张晨阳 663、于跃 0671）
- [x] 4.3 验收必须过：彭伟、陆玉蕾、李扬 8/1–8/12 表有卡的日子，矩阵不得全空漏刷
- [x] 4.4 抽查赵艺娴 8/1–8/13：挪走彭伟的 source_record 后，她自己的卡还在
- [x] 4.5 回放后仍「表有卡系统空」的人单独清单（067x–070x 等），不计入本 change 已清

## 5. 发布

- [x] 5.1 绑定种子 + 回放入口可在宝塔执行，不改得力 00:00/12:00 cron
- [x] 5.2 回放默认关闭，确认名单和窗口后再跑
- [x] 5.3 部署后按 2 → 3 → 4 的顺序在目标库执行，不得只点「重新计算」
