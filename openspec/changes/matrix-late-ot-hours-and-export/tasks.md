## 1. 迟到阈值（8:30 整点即迟到）

- [x] 1.1 计算器 `addLateWithoutAbsence` / `addLateOrConvertToAbsence`：到达时刻 `>=` 段开始即迟到（`!isBefore(start)`）；`08:29:59` 不准时标迟到
- [x] 1.2 恰好 `08:30:00`：raw 分钟为 0、不消费宽限，仍写入迟到事件（rule hit / LATE），供矩阵和迟到统计使用
- [x] 1.3 矩阵 `DayAccumulator.late` 改为看 raw 迟到或上班卡 `>=` 班次开始，禁止只认 `penalizedLateMinutes > 0`；宽限内仍显示 `HH:mm 迟到`
- [x] 1.4 迟到统计次数包含整点迟到（分钟可为 0）
- [x] 1.5 单测：`08:29:59` 准时；`08:30:00` 与 `08:30:30` 格子为 `08:30 迟到`；宽限 15 分钟内计罚为 0 但格子仍迟到

## 2. 休息日首末卡

- [x] 2.1 `selectDayClocks`：休息日（周六/周日/节假日）取当天证据窗口内最早和最晚；工作日保持正午切
- [x] 2.2 组装器休息日两段分别放这两张卡，即使两张都在 12:00 之后（15:00 / 18:00）
- [x] 2.3 休息日超过两张卡时格子只留首尾；中间时刻可进悬停
- [x] 2.4 单测：10:00+12:00 两行都有；15:00+18:00 两行都有；08:30+15:00 仍上下两行；工作日午休中间卡不是下班卡

## 3. 加班色按段、有单即涂

- [x] 3.1 `overtimeTone()`：有生效 OA 加班单即 true，不再要求 `recognizedOvertimeMinutes > 0`；无单晚走不涂绿
- [x] 3.2 `composeSlot`：仅「时间」档且当天有加班单才给加班 tone；上午迟到/假别/漏刷段不被加班绿覆盖
- [x] 3.3 `formulaVersion` 升为 `ATTENDANCE_MONTH_MATRIX_V3`
- [x] 3.4 单测：有单分钟为 0 仍绿；无单 21:38 不绿；`08:32 迟到` + 下午加班绿同时存在；宽限迟到上午仍红

## 4. 悬停假别小时跟班次走

- [x] 4.1 快照为组装器提供每人每天已发布 WORK 段起止（或等价分钟窗口），禁止再用硬编码 `13:00–18:00` 算下午小时
- [x] 4.2 半天悬停小时 = OA 区间 ∩ 该日 WORK 段；拿不到段则不写小时数字，不回退成 5
- [x] 4.3 主表和导出格子文案仍只写「年假」等假名，不含 `4.5` / `5`
- [x] 4.4 单测：扬州夏令 OA `13:00–18:00` 悬停 4.5 不是 5；大连下午悬停 3.5 不是 4.5；全天假悬停不编造两个半天

## 5. 考勤明细 Excel 签到/签退两行

- [x] 5.1 `customerReportWorkbook` 考勤明细：每人两行，身份列竖向合并，增加签到/签退列，对齐 `工作簿11111.xlsx`
- [x] 5.2 每个日期两格独立 fill：签到用 `primaryStatus`，签退用 `secondaryStatus`；删除 `secondaryStatus ?? primaryStatus` 整格一色
- [x] 5.3 页面矩阵保持一人一行、格内两段，不加签到/签退列
- [x] 5.4 前端测试：迟到+加班导出两格两色；全天年假两格都是「年假」无小时；15:00/18:00 休息日两行都有时间

## 6. 回归与发布

- [x] 6.1 跑计算器边界、矩阵组装器、报表导出/mapper 相关测试
- [x] 6.2 发布说明：OPEN 月须重新计算；关账月不自动重开
