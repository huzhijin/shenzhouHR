## 1. Query contracts and menu

- [x] 1.1 为 `missed-punch-stat`、`annual-leave-stat`、`time-off-stat` 增加查询 GET 与导出路径（OpenAPI + 控制器参数），沿用 `ATTENDANCE_REPORT_QUERY:READ`
- [x] 1.2 菜单在忘打卡、年休假、调休额度各自下方插入三项；路由授权与 demo 菜单同步；旧三项路径不变
- [x] 1.3 查询页 sheet 注册三张新表：公共细筛（公司、部门含子部门、员工）+ 各表专属细筛

## 2. 忘打卡统计表

- [x] 2.1 按人分页读取 pin 日格（上午/下午 text、tone、punchAt）；默认排除全月无漏刷且无补签的人
- [x] 2.2 格子绘制：正常时间无底色；漏刷段着色写「漏刷」；补签红字「补签」或「补签HH:mm」；假/休覆盖段不标漏刷
- [x] 2.3 宽表 + 点姓名月历详情；筛选漏刷/已补签/上班漏/下班漏；导出与屏幕一致
- [x] 2.4 现有 `/attendance/queries/missed-punch` 清单页回归：列、筛选、详情仍是事件列表

## 3. 年假与调休统计表

- [x] 3.1 年假统计表按自然年、一人一行：工龄/应休/可休 + 1–12 月已休；新员工折算空为 `0`
- [x] 3.2 调休统计表按自然年、一人一行：期初小时、加班转入、可休 + 1–12 月已休；不含年假工龄档列
- [x] 3.3 分月已休：账期前（现网 2026-01..07）填 `/`，账期起无使用填 `0`，有使用填数量；Excel 相同
- [x] 3.4 详情抽屉读活账户；有 `ANNUAL_LEAVE:ADJUST` 时用现有年假/调休 adjust API（小时、必填原因、幂等）；展示流水；无权限只读
- [x] 3.5 列表读 pin/年报快照，抽屉标明可休以活账户为准；现有年休假、调休额度页回归不变

## 4. Verification

- [x] 4.1 忘打卡统计：正常无底色、单边漏刷只涂一段、补签红字、假日不标漏刷
- [x] 4.2 年假/调休：1–7 月 `/`、8 月未休 `0`、调整后抽屉流水多一行且旧查询页未改
- [x] 4.3 无 pin 不现场核算；无调整权限不能改额度
