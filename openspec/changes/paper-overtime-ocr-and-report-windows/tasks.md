## 1. 同步 cron

- [x] 1.1 将 OA 默认 cron 改为 `0 0 * * * ?`，得力改为 `0 0 0,8,12,18 * * ?`（`application.yml`、Job 注释、env 示例）
- [x] 1.2 更新钉死旧 `0,12` cron 的测试（含 `PinAttendanceReportSnapshotGuardTest`、`OaAutoSyncJobTest`、`DeliAutoSyncJobTest`）
- [x] 1.3 确认调度路径仍走现有水位线增量，不按调度时钟重拉全量

## 2. 自动重算改为近 3 天

- [x] 2.1 去掉 12 小时双源门：OA 或得力任一侧定时成功即可排队
- [x] 2.2 延迟窗口内多次成功合并为一次；默认延迟改为可配 `PT10M`
- [x] 2.3 自动任务调用 LAST_3_DAYS 局部重算，跳过关闭/冻结月
- [x] 2.4 手工同步成功仍不触发自动重算
- [x] 2.5 单测：仅 OA 成功会排队；18 点得力不依赖 12 点槽；关闭月跳过

## 3. 纸质加班表结构与权限

- [x] 3.1 Flyway：`paper_overtime_batch` / `attachment` / `line`，以及加班单据 origin（OA/PAPER）
- [x] 3.2 增加 `PAPER_OVERTIME` 考勤来源（或等价 origin），`source_business_key` = `PAPER:<lineId>`
- [x] 3.3 能力 `PAPER_OVERTIME:MANAGE`（及识别所需读/写）只授 `HR_ADMIN`（`SYSTEM_ADMIN` 按运维需要）
- [x] 3.4 OpenAPI：批次、上传、识别、匹配候选、保存、列表

## 4. OCR 与主表识别

- [x] 4.1 `OcrPort`：百度 accurate API，凭证只读 `SHENZHOUHR_OCR_BAIDU_API_KEY` / `SECRET_KEY`
- [x] 4.2 PDF 按页栅格化；一次请求识别多张图片/PDF
- [x] 4.3 解析姓名、部门、日期（无年默认当年）、起止、事由、加班类型勾选
- [x] 4.4 勾选 0 个、多个或看不清时类型留空，不默认
- [x] 4.5 识别接口不写证据，只返回明细草稿；密钥不得入库
- [x] 4.6 单测：双勾选 → 类型空；三页 PDF → 三行；正反面两图 → 两行不自动合并

## 5. 人与部门匹配

- [x] 5.1 建议接口：精确姓名 + 模糊/包含，部门路径包含与编辑距离
- [x] 5.2 同名全部返回（工号、标准部门、在职），按部门接近度排序，不隔离
- [x] 5.3 保存只接受人事选定的 `employeeId`；部门与工号以花名册为准
- [x] 5.4 得力/OA 无人值守解析保持原隔离策略
- [x] 5.5 单测：两个陈士庆都列出；OCR「设备部」推荐「设备工程部」仍可改选

## 6. 保存门槛与证据入库

- [x] 6.1 类型空、人未确认、缺日期/起止 → 整批失败，无 force-save 字段
- [x] 6.2 同人重叠自然日（含过夜占到的每天、已有 OA/纸质、同批明细）报错并指出冲突单
- [x] 6.3 通过后每行 ingest 为 APPROVED `OVERTIME` 证据，调休走与 OA 相同 TIME_OFF 入账
- [x] 6.4 小时用现有 `OaIntervalGrid` + `OvertimeMealDeductions`，不按附件份数相加
- [x] 6.5 单测：同日第二张失败；过夜占次日失败；同名不同工号可存；无卡纸质晚间单仍出 2.5h

## 7. 纸质加班前端（主表 + 明细）

- [x] 7.1 菜单：人事可见「纸质加班单」；主表附件+识别，明细一单一行，可新增手填行
- [x] 7.2 识别后带出字段与候选人；选人/换人后带出工号与部门
- [x] 7.3 保存错误展示冲突日期与已有来源；空类型行不能存
- [x] 7.4 同一主表可挂多文件；删重复明细行后附件仍留在主表

## 8. 加班改单据明细

- [x] 8.1 查询加班统计列出 OA + 纸质，一行一张单，带来源列
- [x] 8.2 报表中心加班页改为单据明细，去掉部门汇总和一人一行合计
- [x] 8.3 导出跟明细行；月度工时加班列仍为日事实加总
- [x] 8.4 前端/API 测试：一人四天四行；工时合计仍加总

## 9. 报表起止筛选

- [x] 9.1 报表中心增加起止日期（可跨月、不必整月）
- [x] 9.2 展示用日事实/单据与窗口相交；窗口外日事实仍留在 pin
- [x] 9.3 顶卡与表内合计按窗口重加，不把窗口外天数加进去
- [x] 9.4 查询加班统计保持已有起止，并含纸质行

## 10. 近 3 天 / 近一周 / 本月重算

- [x] 10.1 `RecalculateRequest` 增加 `window`: `LAST_3_DAYS` | `LAST_7_DAYS` | `MONTH`，相对今天（上海）
- [x] 10.2 只重算窗口内日事实；窗口外保留；多看 1 天跨夜上下文但不写回窗口外
- [x] 10.3 月宽限用窗口前已存日事实计数后再算窗口
- [x] 10.4 合并后重加月合计/工时公式列，发布新 projectionVersion
- [x] 10.5 跨月：OPEN 月写入，CLOSED/FROZEN 跳过并在响应说明
- [x] 10.6 前端三个按钮替换原「重新计算」；选中过去月份时「近 3 天」仍相对今天
- [x] 10.7 单测：8-26 近 3 天只换 24–26 日；7 月关闭则 8-01 近 3 天只写 8 月；本月按钮对关账月拒绝

## 11. 回归与发布

- [x] 11.1 同步 cron、自动重算、纸质保存/重叠、局部重算、加班明细相关测试全绿
- [x] 11.2 部署说明：新 cron、OCR 环境变量占位、OPEN 月需重算后纸质单才进 pin
- [x] 11.3 确认仓库无 OCR 密钥
