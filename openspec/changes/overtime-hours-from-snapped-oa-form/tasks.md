## 1. 餐扣纯函数

- [x] 1.1 新增 `OvertimeMealDeductions`：取整后区间完全盖住餐窗才扣；周末/节假日 `12:00–13:00` 扣 60；夏令晚餐 `18:00–18:30` 扣 30；冬令晚餐为班次下班起 30 分钟，无班次则 `17:30–18:00`
- [x] 1.2 过夜单按 Asia/Shanghai 日历日切开后分别判断当天餐窗，再把扣减加总
- [x] 1.3 单测：周六 `08:30–17:00` 扣 60；周六 `18:00–21:00` 夏令扣晚餐 30、不扣午休；工作日 `18:00–21:00` 夏令 2.5h；`18:30–21:00` 不扣晚餐；`18:45–21:10` 先 snap 再算 2.5h；`18:00–18:17` 为 0；`12:00–21:00` 夏令周六 7.5h

## 2. OA 加班 recognized_minutes

- [x] 2.1 `recognizedMinutes` 对 OVERTIME 停止读取 `overtimeBySourceKey`（打卡配对结果）
- [x] 2.2 改为 snap 区间毛分钟减去 `OvertimeMealDeductions`；snap 失败为 0，行仍写入取整后起止
- [x] 2.3 虚假加班仍投影异常，不得把该单 `recognized_minutes` 写成 0
- [x] 2.4 单测：无打卡的工作日 `18:00–21:00` 夏令小时 2.5；奇数打卡的周六白天单仍按单据扣午休出小时

## 3. 日事实与工时对齐

- [x] 3.1 日事实 `paid/compensatory/voluntary_overtime_minutes` 改为按该日摊到的单据分钟汇总（同一套取整+餐扣）
- [x] 3.2 查询加班统计小时与月度工时计薪/转调休对同一人同一月一致
- [x] 3.3 单测：一张计薪单 2.5h 同时出现在加班统计和工时计薪加班

## 4. 展示

- [x] 4.1 加班统计开始/结束继续用 snap 后时刻；小时为 0.5 的倍数（含 0）
- [x] 4.2 禁止再出现 `1.3333333333333333` 这类 double 尾巴；必要时查询页对加班小时按一位小数且为 0.5 步长格式化
- [x] 4.3 前端或 API 测试：150 分钟显示 `2.5`，0 显示 `0`

## 5. 回归与发布

- [x] 5.1 加班识别、餐扣、日事实、查询页相关后端测试全绿
- [x] 5.2 对照现场：张珍珍类有卡仍为 0 的晚上单变为单据时长减餐；周六晚上 `18:00–21:00` 为 2.5 不扣午休
- [x] 5.3 发布说明：OPEN 月须重新计算；关账月不自动重开
