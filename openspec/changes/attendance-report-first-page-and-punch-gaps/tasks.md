## 1. 考勤报表首屏：后端按页读 pin

- [x] 1.1 官方 `GET /month-matrix` 在已有 pin 时只解析一次授权和 projectionId，禁止走整月 `loadAuthorizedSnapshot` 再内存切片
- [x] 1.2 员工列表 SQL 分页：`page=0&size=50` 只取 50 人；响应带 `totalEmployees`（日事实员工 ∪ 当月有效 OA 员工）
- [x] 1.3 日格子 / OA / 异常只查本页 `employee_id IN (...)`；`organizationId` 为空时不跑递归部门子查询、不对每行套 `reportFactVisibility`
- [x] 1.4 有 pin 时 GET 不得等待 `FIRST_WAIT` 核算线程；无 pin 仍返回可重试「尚未完成」，加载中不得写成「暂无记录」
- [x] 1.5 契约/单测：page0 不加载 page1 员工；公司范围查询不含递归 CTE；江苏神州体量下 page0 目标 ≤ 5 秒（现网验收）

## 2. 考勤报表首屏：前端先出 50 人再灌满

- [x] 2.1 去掉 `collectMatrixPages`「不选人就第一页截断」；第一页画出来后继续请求后续页，绑定同一 `expectedProjectionVersion`
- [x] 2.2 pin 中途变更则从第 0 页重拉，不把两份投影拼在一张表上
- [x] 2.3 灌数未完成：横幅写预览；KPI/「共 N 名」不把全 0 当定稿；完成后刷新全量人数
- [x] 2.4 不选人时禁止再提示「超过最大加载行数」；只选一人时仍一次请求、不再翻全公司
- [x] 2.5 前端测试：551 人会翻完；50 人页先渲染；截断文案不再出现在公司全量视图

## 3. 格子：OA 色、漏刷、休息日打卡

- [x] 3.1 矩阵员工集合 = 日事实员工 ∪ 当月有效 OA 且在花名册的人（霍岩 `SZST0445` 必须有行）
- [x] 3.2 有生效 OA 的日期用单据颜色，不得因为没卡画成漏刷
- [x] 3.3 前端 `primaryStatusOrder`：加班/外出/请假等单据色排在 `REST_DAY` 之前（霍岩 8/1 周六加班色）
- [x] 3.4 无日事实的应出勤工作日空卡记漏刷（霍岩除加班日外）
- [x] 3.5 休息日打卡写入 `first_punch_at`/`last_punch_at`（即使应出勤分钟为 0）；彭伟 8/8 有 Excel 时刻则格子有时刻
- [x] 3.6 休息日有加班单：加班色 + 有卡出时刻；无加班单也要出打卡，不能空

## 4. 认人：花名册工号唯一 + 规则 B + 同号姓名重挂

- [x] 4.1 绑定键改为花名册工号唯一；已有工号以 HR 姓名为准
- [x] 4.2 同工号姓名冲突重挂：0663 张晨阳 / 黄凯→0667；0302 姜长波 / 张海兰→0303；0664 周易钊；0669 不占周易钊
- [x] 4.3 规则 B：得力工号不在花名册、姓名恰好一人 → 挂到该正式工（张国庆、赵建浩、陈希雨、汪婷婷、程兆俊、葛倩荣、蔡小钰）
- [x] 4.4 花名册已有该工号时禁止用姓名抢绑；重名不匹配
- [x] 4.5 系统免打卡（王鲲鹏/苑磊/黄凯后半段）只挂卡，不改常驻名单/高管规则
- [x] 4.6 单测覆盖：B 七人、张海兰 0303、0663 冲突、重名不绑、已有工号不抢

## 5. 回放、重算、对账

- [x] 5.1 绑定写入后回放 2026-08 得力窗口（错挂/隔离改挂，同一 `source_record_id` 不双计）
- [x] 5.2 回放后重算江苏神州 2026-08；不改得力 00:00/12:00 cron；不得只点重新计算当认人完成
- [ ] 5.3 季佳男 `SZST0105`：两份 Excel 有卡则日事实必须有卡（工号已一致，查原始 CHECKIN）
- [x] 5.4 聚能等：能匹配到 HR 的人出现在其公司报表；匹配不上的不塞进江苏神州
- [x] 5.5 07xx：保留原始卡，本轮不导入花名册

## 6. 明确跳过（todo，等你确认后再开）

- [x] 6.1 记下但不实现：邵怀芳、刘倩、望天荷
- [x] 6.2 记下但不实现：徐利民 `SZST0674`、黄兆隆 `SZST0662`

## 8. 报表筛选可清空回全部

- [x] 8.1 考勤报表部门、员工：选中后可清空或回到「全部部门 / 全部员工」（allowClear；清空不得被 onChange 忽略）
- [x] 8.2 查询报表各部门、员工筛选同样可清空回全部（含 TreeSelect / Select）
- [x] 8.3 测试：清空后请求不再带 organizationId / employeeId

## 9. 导出（纳入本部署包）

- [x] 9.1 部署包带上查询页与考勤报表中心的 Excel 导出（与展示字段一致）
- [x] 9.2 升级脚本同步 jar + 前端，启动后健康检查走 9090

## 7. 现网验收（江苏神州 2026-08，只看考勤报表）

- [ ] 7.1 不选人打开总表：第一页 50 人 3–5 秒内可看，随后灌满约 551 人
- [ ] 7.2 霍岩有行；8/1 加班色；其余应出勤无卡为漏刷
- [ ] 7.3 彭伟工作日仍对上；8/8 有卡不空
- [ ] 7.4 张海兰 `SZST0303`、规则 B 七人、季佳男：Excel 有时刻的工作日格子有卡
- [ ] 7.5 浏览器硬刷新总表走完上述路径，不引导去查询报表
