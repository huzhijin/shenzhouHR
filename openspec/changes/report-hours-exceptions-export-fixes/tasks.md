## 1. OA 30 分钟网格

- [x] 1.1 新增 `OaIntervalGrid.snap`：Asia/Shanghai，分钟 0–29→整点，30–59→半点，已是 :00/:30 不变，秒清零
- [x] 1.2 `OaDocumentConverter` 把请假/加班/调休/外出/出差区间两端 snap 后再交给计算器
- [x] 1.3 `projectOaReportFacts` 写入的 interval start/end 与 recognized 计算使用同一 snap 区间
- [x] 1.4 单测：09:17→09:00、18:45→18:30、13:00/17:30 不变、23:50–07:20→23:30–07:00、09:00–09:17 认可分钟为 0
- [x] 1.5 提升公式目录版本，使未重算的旧 pin 不再被当成新口径

## 2. 加班小时按单据区间认定

- [x] 2.1 `recognizedScheduledMinutes` 对 OVERTIME 停止与 WORK 班段求交
- [x] 2.2 加班单 `recognized_minutes` 改为该证据在日结果中的认可加班分钟（计薪/调休/义务分类保留）
- [x] 2.3 计算器加班证据使用 snap 后区间，休息日无班段时打卡落在单内仍可 > 0；午餐/晚餐/虚假加班规则不变
- [x] 2.4 加班查询页 开始/结束输出 snap 后时刻，小时用认可分钟，禁止空「—」且实际有单有卡却为 0.00
- [x] 2.5 单测：周六 09:00–17:30 有卡有单小时 > 0；工作日 18:00–21:00 班后加班不依赖与 08:30–17:30 相交

## 3. 月度工时口径

- [x] 3.1 查询工时：请假小时 = 事假+病假+其他，年假/调休单独列；实际工时用官方公式
- [x] 3.2 工时备注改为入职/离职或空，SQL 去掉 `MAX(shift_label) AS employmentNote`
- [x] 3.3 有应出勤小时的人备注不得显示「无班次」
- [x] 3.4 单测：年假 4.5 不重复计入请假小时；128 应出勤 + 1.5 计薪加班 − 4.5 年假 与公式一致

## 4. 异常去重与假单抑制

- [x] 4.1 投影按 `(employeeId, date, 展示类型)` 折叠；`MISSING_PUNCH_*` 投影为上班缺卡或下班缺卡，同一侧一行
- [x] 4.2 去掉会再插一行缺卡的 metric fallback，或与已有 `MISSING_*` 互斥
- [x] 4.3 有效请假/调休/外出/出差/免打卡盖住上午则抑制上班缺卡和迟到；盖住下午则抑制下班缺卡和早退；盖住全天则抑制旷工和双侧缺卡
- [x] 4.4 查询异常页只返回 actionable 类型，且结果去重
- [x] 4.5 单测：全天事假不出缺卡/迟到/早退/旷工；同一人同一日不会出现三行「缺卡」
- [x] 4.6 发布说明：OPEN 月重新计算后新 pin 替换重复行，不做手工 DELETE

## 5. 忘打卡次数

- [x] 5.1 忘打卡查询筛选缺卡家族（PENDING/OVERDUE/ON_DUTY/OFF_DUTY），禁止 `= 'MISSING_PUNCH'` 精确匹配把行滤空
- [x] 5.2 列表按 `(员工, 日期, 侧)` 去重；全天无卡无单据计 2，单边计 1
- [x] 5.3 工作台类型分布按去重后的缺卡侧计数，重复事实不得把 3 次打成 24
- [x] 5.4 单测：上午请假 + 缺下班卡只计 1；工作台 COUNT 与去重后侧数一致

## 6. 查询页导出

- [x] 6.1 查询接口响应补齐 `queryFingerprint`、`scopeReference`（及导出所需的 projection 绑定）
- [x] 6.2 前端导出携带完整绑定、筛选、中文 purpose，轮询 READY 后下载 xlsx，失败留在当前页提示
- [x] 6.3 导出创建：`ATTENDANCE_REPORT_QUERY:READ` + `EXPORT_CREATE` 即可，不再强制 `ATTENDANCE_REPORT:READ`
- [x] 6.4 无 `EXPORT_CREATE` 时隐藏导出按钮；下载仍要 `EXPORT_DOWNLOAD`
- [x] 6.5 单测：完整请求成功；缺 READ 但有查询+导出能力成功；残缺 body 不得被前端发出

## 7. 查询页展示

- [x] 7.1 `detailEntries` 只输出 `sheetColumns` 白名单中文列，删除 `Object.keys(row)` 英文兜底
- [x] 7.2 只有 `action` 列渲染「查看详情」；`note`/`details` 显示文本或「—」
- [x] 7.3 月度工时表格增加「年假小时」「调休小时」；迟到增加「迟到分钟」「计罚迟到分钟」，去掉空说明列或固定为 —
- [x] 7.4 前端测试：工时/迟到抽屉不含 `annualLeaveHours`、`timeOffHours`、`lateMinutes`、`penalizedLateMinutes`；备注列不是第二颗查看按钮

## 8. 回归与验收

- [x] 8.1 加班、请假、工时、异常投影、忘打卡、导出权限相关后端测试全绿
- [x] 8.2 查询页与报表中心前端测试全绿
- [x] 8.3 对照现场案例：休息日加班小时非 0、异常无重复、忘打卡次数、导出 xlsx、工时/迟到抽屉无英文
- [x] 8.4 列出部署后需对 OPEN 月执行「重新计算」的操作说明（关账月不自动重开）
