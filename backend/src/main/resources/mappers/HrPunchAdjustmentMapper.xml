<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.szsemicon.hr.reporting.infrastructure.persistence.HrPunchAdjustmentMapper">

    <insert id="insert">
        INSERT INTO attendance_hr_punch_adjustment (
            adjustment_id,
            company_id,
            employee_id,
            business_date,
            on_duty_at,
            off_duty_at,
            reason,
            created_by,
            created_at,
            overtime_minutes_override,
            cleared_exception_types,
            day_types
        ) VALUES (
            #{adjustmentId},
            #{companyId},
            #{employeeId},
            #{businessDate},
            #{onDutyAt},
            #{offDutyAt},
            #{reason},
            #{createdBy},
            #{createdAt},
            #{overtimeMinutesOverride},
            #{clearedExceptionTypes},
            #{dayTypes}
        )
    </insert>

    <select id="findEmployeeCompanyId" resultType="string">
        SELECT company_id
        FROM employee
        WHERE employee_id = #{employeeId}
    </select>
</mapper>
