# Frontend notes — shenzhouHR

> Instruction checklist for the interactive worker session.
> Open Claude Code / Codex / OpenCode in this workspace and follow each item.

## Sources of truth

- `output/shenzhouHR-prd.md` (acceptance criteria)
- `output/shenzhouHR-architecture.md` (API surface)
- `output/shenzhouHR-uiux.md` (design tokens + page hierarchy)

## Build & verify checklist

- [ ] icon library declared and imported (Lucide / Heroicons / Tabler)
- [ ] color tokens loaded from `output/shenzhouHR-uiux.md`
- [ ] every `fetch` URL appears in `output/shenzhouHR-architecture.md`
- [ ] runtime smoke screenshot attached for review

## Preview URL

_(The worker fills this with the local URL its dev server printed,
e.g. `http://localhost:5173`. UmaDev opens it for the user.)_

## Run command

_(e.g. `cd web && npm run dev`)_